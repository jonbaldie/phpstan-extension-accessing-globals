Instructions for interpreting errors
---------

Each error has an associated identifier, like `argument.type`
or `return.missing`.

Each error identifier has documentation at URL https://phpstan.org/error-identifiers/<identifier>
This page contains code example, explanation why is this an error
and instruction how to fix it.
Before fixing the error, fetch the documentation page for its identifier.

The error usually indicates a real bug or incorrect type in the code. Fix the underlying cause, do not just make the error go away.
Do not add `@phpstan-ignore` comments, `@phpstan-ignore-next-line` comments, or baseline entries to suppress the error.
Do not use assert() or inline @var PHPDoc tag to override PHPStan's inferred type.
Do not add type casts just to silence errors.
Do not widen parameter or return types just to make the error go away.
 ------ -----------------------------------------------------------------------
  Line   access-superglobals.php
 ------ -----------------------------------------------------------------------
  5      Code is accessing superglobal variable $_GET. Pass the value as an
         argument instead.
         🪪  access.superglobal
  6      Code is accessing superglobal variable $_POST. Pass the value as an
         argument instead.
         🪪  access.superglobal
  7      Code is accessing superglobal variable $_REQUEST. Pass the value as
         an argument instead.
         🪪  access.superglobal
  8      Code is accessing superglobal variable $_SESSION. Pass the value as
         an argument instead.
         🪪  access.superglobal
  9      Code is accessing superglobal variable $_COOKIE. Pass the value as an
         argument instead.
         🪪  access.superglobal
  10     Code is accessing superglobal variable $_FILES. Pass the value as an
         argument instead.
         🪪  access.superglobal
  11     Code is accessing superglobal variable $_ENV. Pass the value as an
         argument instead.
         🪪  access.superglobal
  12     Code is accessing superglobal variable $_SERVER. Pass the value as an
         argument instead.
         🪪  access.superglobal
  13     Code is accessing superglobal variable $GLOBALS. Pass the value as an
         argument instead.
         🪪  access.superglobal
 ------ -----------------------------------------------------------------------

 ------ -----------------------------------------------------------------------
  Line   modify-superglobals.php
 ------ -----------------------------------------------------------------------
  5      Code is accessing superglobal variable $_GET. Pass the value as an
         argument instead.
         🪪  access.superglobal
  5      Code is modifying superglobal variable $_GET. Return the new value
         instead.
         🪪  modify.superglobal
  6      Code is accessing superglobal variable $_POST. Pass the value as an
         argument instead.
         🪪  access.superglobal
  6      Code is modifying superglobal variable $_POST. Return the new value
         instead.
         🪪  modify.superglobal
  7      Code is accessing superglobal variable $_REQUEST. Pass the value as
         an argument instead.
         🪪  access.superglobal
  7      Code is modifying superglobal variable $_REQUEST. Return the new
         value instead.
         🪪  modify.superglobal
  8      Code is accessing superglobal variable $_SESSION. Pass the value as
         an argument instead.
         🪪  access.superglobal
  8      Code is modifying superglobal variable $_SESSION. Return the new
         value instead.
         🪪  modify.superglobal
  9      Code is accessing superglobal variable $_COOKIE. Pass the value as an
         argument instead.
         🪪  access.superglobal
  9      Code is modifying superglobal variable $_COOKIE. Return the new value
         instead.
         🪪  modify.superglobal
  10     Code is accessing superglobal variable $_FILES. Pass the value as an
         argument instead.
         🪪  access.superglobal
  10     Code is modifying superglobal variable $_FILES. Return the new value
         instead.
         🪪  modify.superglobal
  11     Code is accessing superglobal variable $_ENV. Pass the value as an
         argument instead.
         🪪  access.superglobal
  11     Code is modifying superglobal variable $_ENV. Return the new value
         instead.
         🪪  modify.superglobal
  12     Code is accessing superglobal variable $_SERVER. Pass the value as an
         argument instead.
         🪪  access.superglobal
  12     Code is modifying superglobal variable $_SERVER. Return the new value
         instead.
         🪪  modify.superglobal
  13     Code is accessing superglobal variable $GLOBALS. Pass the value as an
         argument instead.
         🪪  access.superglobal
  13     Code is modifying global variable through $GLOBALS['foo']. Use
         dependency injection instead.
         🪪  modify.global
  13     Code is modifying superglobal variable $GLOBALS. Return the new value
         instead.
         🪪  modify.superglobal
 ------ -----------------------------------------------------------------------

 [ERROR] Found 28 errors


exit_code=1
